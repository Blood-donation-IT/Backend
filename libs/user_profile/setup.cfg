[metadata]
name = user_profile_models
version = 0.1.0
description = Shared user profile models
author = Makar
license = MIT

[options]
packages = find_namespace:
package_dir =
    = src
include_package_data = True
zip_safe = False

[options.packages.find]
where = src