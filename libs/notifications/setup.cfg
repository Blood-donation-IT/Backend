[metadata]
name = notifications_models
version = 0.1.0
description = Shared notifications models
author = Danya
license = MIT

[options]
packages = find_namespace:
package_dir =
    = src
include_package_data = True
zip_safe = False

[options.packages.find]
where = src
